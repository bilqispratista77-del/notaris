[phases.setup]
nixPkgs = ["...", "nodejs", "nodePackages.pnpm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "node .next/standalone/server.js"
